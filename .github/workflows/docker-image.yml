name: AIML1_Docker

on:
  push:
    branches: [ "Madalina" ]
  pull_request:
    branches: [ "Madalina" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # Step 1: Checkout the code
    - uses: actions/checkout@v4

    # Step 2: Log in to Docker Hub
    - name: Log in to Docker Hub
      run: echo ${{ secrets.DOCKERHUB_TOKEN }} | docker login -u ${{ secrets.DOCKERHUB_USERNAME }} --password-stdin

    # Step 3: Build the Docker image without cache
    - name: Build the Docker image
      id: build_image
      run: |
        IMAGE_TAG=$(date +%s)
        docker build --no-cache . --file Dockerfile --tag aiml1_docker/my-image-name:$IMAGE_TAG
        echo "IMAGE_TAG=$IMAGE_TAG" >> $GITHUB_ENV

    # Step 4: Tag the image with 'latest'
    - name: Tag the Docker image
      run: docker tag aiml1_docker/my-image-name:${{ env.IMAGE_TAG }} aiml1_docker/my-image-name:latest

    # Step 5: Push the Docker image to DockerHub
    - name: Push Docker image to DockerHub
      run: |
        docker push aiml1_docker/my-image-name:${{ env.IMAGE_TAG }}
        docker push aiml1_docker/my-image-name:latest
